from src.schemas import AgentState

def reconcile_findings(state: AgentState) -> AgentState:
    """
    Node 4: Reconcile and format output (skeleton)
    """
    print("Node 4: Reconciling findings...")

    # TODO: Format findings as JSON and Markdown
    # For now, return empty output
    return {
        'final_json': {},
        'final_markdown': ""
    }
